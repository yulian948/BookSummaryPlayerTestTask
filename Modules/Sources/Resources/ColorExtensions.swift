import UIKit
import SwiftUI

public extension Color {
    static let accent = Color(UIColor(named: "AccentColor", in: .module, compatibleWith: nil)!)
    static let playerBlue = Color(UIColor(named: "PlayerBlueColor", in: .module, compatibleWith: nil)!)
    static let playerButtonForeground = Color(UIColor(named: "PlayerButtonForegroundColor", in: .module, compatibleWith: nil)!)
    static let playerGray = Color(UIColor(named: "PlayerGrayColor", in: .module, compatibleWith: nil)!)
}
